package controllers

import (
	"github.com/astaxie/beego"
)

type Controller struct {
	beego.Controller
}

type RoomController struct {
	beego.Controller
}

type HomeController struct {
	beego.Controller
}
